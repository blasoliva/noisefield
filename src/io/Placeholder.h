#pragma once

// Placeholder for milestone M4 IO code: WAV/FLAC recorder, offline (faster-than-real-time)
// exporter, and the headless CLI renderer. Kept so the directory and its CMake target exist
// from M1.

namespace noisefield::io
{
} // namespace noisefield::io
